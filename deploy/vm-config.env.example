# HealthCure - Azure VM Deployment Configuration Template
# 
# INSTRUCTIONS:
# 1. Copy file ini menjadi vm-config.env
# 2. Update values dengan credentials Azure VM Anda
# 3. File vm-config.env JANGAN di-commit ke repository (sudah di .gitignore)
# 
# cp vm-config.env.example vm-config.env
# 

# ============================================
# AZURE VM CREDENTIALS (Required)
# ============================================
# Username untuk SSH ke Azure VM
VM_USERNAME=azureuser

# Password untuk SSH ke Azure VM
VM_PASSWORD=YourSecurePasswordHere123!

# Public IP address dari Azure VM
VM_PUBLIC_IP=1.2.3.4

# SSH port (default: 22)
VM_SSH_PORT=22

# SSH private key path (optional, gunakan jika prefer key-based auth)
# VM_SSH_KEY_PATH=~/.ssh/id_rsa


# ============================================
# DEPLOYMENT CONFIGURATION
# ============================================
# Target directory di VM untuk ekstrak project
DEPLOYMENT_PATH=/home/azureuser/healthcure

# Docker Compose port mapping untuk frontend
APP_PORT=3000

# Docker Compose version
DOCKER_COMPOSE_VERSION=2.0


# ============================================
# APPLICATION CREDENTIALS (Optional)
# Gunakan jika ingin custom dari default
# ============================================
# Admin email untuk login
ADMIN_EMAIL=admin@healthcure.com

# Admin password untuk login
ADMIN_PASSWORD=admin123

# JWT secret key
JWT_SECRET=your-secret-key-change-this-in-production


# ============================================
# REPOSITORY (Optional)
# ============================================
# GitHub repository URL
GITHUB_REPO=https://github.com/YOUR_GITHUB_USERNAME/sistem-data-pasien.git

# Git branch untuk deployment
GITHUB_BRANCH=main
